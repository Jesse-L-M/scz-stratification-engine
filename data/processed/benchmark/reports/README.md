# processed/benchmark/reports

Store generated benchmark audit reports here.

Current expected outputs from `scz-audit benchmark audit-datasets`:

- `dataset_audit.json`
- `dataset_audit.md`

These checked-in reports should stay deterministic across reruns and should not
embed generated timestamps or machine-local paths.
