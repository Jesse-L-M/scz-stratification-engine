# examples/benchmark_v0

Place small, public benchmark examples here.

Keep this directory lightweight so later PRs can add smoke-test configs, manifests, or toy assets without shipping real cohorts.
